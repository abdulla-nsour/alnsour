!function (n) {
    "function" == typeof define && define.amd ? define(["jquery"],) : "object" == typeof module && "object" == typeof module.exports ? exports = n(require("jquery")) : n(jQuery)
}
    (
        function (n) {
            var t = Math.pow;
            n.extend(n.easing, {
                def: "easeOutQuad",
                easeInOutExpo: function (n) {
                    return 0 === n ? 0 : 1 === n ? 1 : n < .5 ? t(2, 20 * n - 10) / 2 : (2 - t(2, -20 * n + 10)) / 2
                },
            })
        }
    );
